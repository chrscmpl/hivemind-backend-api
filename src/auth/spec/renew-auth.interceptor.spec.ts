import { RenewAuthInterceptor } from '../interceptors/renew-auth.interceptor';

describe('RenewAuthInterceptor', () => {
  it('should be defined', () => {
    expect(new RenewAuthInterceptor()).toBeDefined();
  });
});
